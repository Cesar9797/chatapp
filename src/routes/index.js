const userRoutes = require('./users.routes');
const authRoutes = require('./auth.routes');
const conversationRoutes = require('./conversations.routers');

module.exports = {
  userRoutes,
  authRoutes,
  conversationRoutes
}